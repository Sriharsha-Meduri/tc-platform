import type { FormFieldTemplate } from './field-analysis.types';

// Re-export from the template coordinates service (single source of truth)
export { getFormTemplate } from './template-coordinates.service';
