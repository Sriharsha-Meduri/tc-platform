Document Signed via DocuSign

Hello,

{{#if propertyAddress}}For the transaction at {{propertyAddress}}, the {{else}}The {{/if}}following document has been fully signed through DocuSign:

{{documentName}}
{{#if formCode}}Form: {{formCode}}
{{/if}}
The signed copy has been saved to the transaction and now replaces the unsigned version everywhere it appears — the secure upload links and the transaction document list will show the signed copy.

myTC
Automating Every Step After Contract Acceptance

---
{{#if transactionId}}Transaction ID: {{transactionId}}
{{/if}}
